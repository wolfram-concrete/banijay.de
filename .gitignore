# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Lokales Quellmaterial (Fonts, Film, Visuals, scraped_content) — nicht deploybar, zu gross fuer GitHub
# (genutzte Assets liegen als optimierte Kopien in public/ und src/app/fonts/)
design-source/
assets/
scraped_content/
.venv
# Ungenutzte/zu große Rohassets bleiben ignoriert:
preloader.png
stage-portrait.png
hero-cinematic.mp4
# Genutzte Videos werden committet (weboptimiert), damit sie im Vercel-Deploy
# vorhanden sind: kompetenz-reel, hero, hero-bg, preloader-bg, team-fullscreen,
# grid-loop1-3 (siehe public/video/).

# Uebersichts-/Snapshot-PDFs (Deliverables, nicht ins Repo)
banijay-website-uebersicht.pdf

# Font-Testseite (Lizenzen ungeklärt — nicht committen)
public/fonts-test/
