# banijay Hero Animation

Standalone static deployment for the banijay hero animation.

- Entry: `index.html`
- Required asset: `assets/background-neon-stage.png`
- No build step required

The animation loads Three.js from the public CDN at runtime.
